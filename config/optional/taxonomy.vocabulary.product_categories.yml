langcode: en
status: true
dependencies:
  module:
    - barcode_scanner
  enforced:
    module:
      - barcode_scanner
name: 'Barcode Scanner: Product Categories'
vid: product_categories
description: 'Group scanned items into categories.'
weight: 0
